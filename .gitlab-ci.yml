default:
    image: node:default
 
stages:
    - test

test:
    stage: test
    script:
        - npm -v
        - node -v
        - pwd
        - cd Frontend
        - pwd
        - npm install
        - CI=true npm test 
