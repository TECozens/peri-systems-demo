default:
    image: node:14
 
stages:
    - test

test:
    stage: test
    script:
        - npm -v
        - echo "NODE VERSION"
        - node -v
        - pwd
        - cd Frontend
        - pwd
        - npm install
        - CI=true npm test 
